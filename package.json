{
  "name": "@shopify/images",
  "version": "1.1.3",
  "description": "Packages to help us wrangle images at Shopify.",
  "license": "MIT",
  "typings": "index.d.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "test": "jest --config ./config/jest/config.json",
    "test:watch": "yarn run test -- --watch",
    "check": "yarn run lint && yarn run test"
  },
  "eslintConfig": {
    "extends": [
      "plugin:shopify/esnext",
      "plugin:shopify/node"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shopify/images.git"
  },
  "devDependencies": {
    "babel-jest": "^20.0.3",
    "babel-preset-shopify": "^16.0.2",
    "eslint": "^3.19.0",
    "eslint-plugin-shopify": "^16.0.0",
    "jest": "^20.0.4",
    "svgo": "^0.7.2"
  }
}
