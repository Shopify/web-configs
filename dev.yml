name: web-configs
up:
  - node:
      version: v22.14.0
      yarn: v1.22.5
commands:
  __default__: start
  check: yarnpkg check
  lint: yarnpkg lint
  start: echo "Running web-configs is not really a concept that makes sense."
  test: yarnpkg test
  generate: yarnpkg generate
