name: web-configs
up:
  - node:
      version: 16.13.0
      yarn: 1.22.15
commands:
  __default__: start
  check: yarnpkg check
  lint: yarnpkg lint
  start: echo "Running web-configs is not really a concept that makes sense."
  test: yarnpkg test
  generate: yarnpkg generate
